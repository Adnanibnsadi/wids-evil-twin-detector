# Visualizations

To generate high-resolution presentation charts for your specific network environment, run the chart generator script after collecting local data:

```bash
sudo ../venv/bin/python3 ../scripts/generate_charts.py
